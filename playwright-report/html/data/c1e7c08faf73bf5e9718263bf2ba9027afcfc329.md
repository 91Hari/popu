# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: customer.spec.ts >> Customer Flow >> my orders page loads
- Location: tests/e2e/customer.spec.ts:42:7

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected pattern: /\/customer/
Received string:  "http://localhost:5173/login"
Timeout: 15000ms

Call log:
  - Expect "toHaveURL" with timeout 15000ms
    34 × unexpected value "http://localhost:5173/login"

```

```yaml
- img "PO.PU"
- text: PO.PU pure · fresh · trusted
- heading "Welcome back!" [level=5]
- paragraph: Sign in with your email or mobile number
- alert: "{\"error\":\"email and password are required\"}"
- text: Email or Mobile Number
- textbox "Email or Mobile Number":
  - /placeholder: email@example.com or 10-digit mobile
  - text: customer@test.com
- text: Password
- textbox "Password": test123
- button
- link "Forgot password?":
  - /url: /forgot-password
- button "Sign In"
- paragraph:
  - text: Don't have an account?
  - link "Register":
    - /url: /register
```

# Test source

```ts
  1  | import { test, expect, Page } from '@playwright/test';
  2  | 
  3  | const EMAIL    = process.env.PLAYWRIGHT_CUSTOMER_EMAIL    || '';
  4  | const PASSWORD = process.env.PLAYWRIGHT_CUSTOMER_PASSWORD || '';
  5  | 
  6  | async function loginAsCustomer(page: Page) {
  7  |   await page.goto('/login');
  8  |   await page.getByLabel(/email or mobile/i).fill(EMAIL);
  9  |   await page.getByLabel(/password/i).fill(PASSWORD);
  10 |   await page.getByRole('button', { name: /sign in|log in/i }).click();
> 11 |   await expect(page).toHaveURL(/\/customer/, { timeout: 15000 });
     |                      ^ Error: expect(page).toHaveURL(expected) failed
  12 | }
  13 | 
  14 | test.describe('Customer Flow', () => {
  15 | 
  16 |   test.skip(!EMAIL, 'PLAYWRIGHT_CUSTOMER_EMAIL not set — skipping customer tests');
  17 | 
  18 |   test('customer dashboard loads', async ({ page }) => {
  19 |     await loginAsCustomer(page);
  20 |     await expect(page).toHaveURL(/\/customer/);
  21 |     await expect(page.getByText(/welcome|dashboard|food|service/i).first()).toBeVisible({ timeout: 10000 });
  22 |   });
  23 | 
  24 |   test('services page renders available services', async ({ page }) => {
  25 |     await loginAsCustomer(page);
  26 |     await page.goto('/services');
  27 |     await expect(page.getByText(/service|food|catering/i).first()).toBeVisible({ timeout: 10000 });
  28 |   });
  29 | 
  30 |   test('food marketplace page loads', async ({ page }) => {
  31 |     await loginAsCustomer(page);
  32 |     await page.goto('/services/food-marketplace');
  33 |     await expect(page.getByText(/food|marketplace|item/i).first()).toBeVisible({ timeout: 10000 });
  34 |   });
  35 | 
  36 |   test('cart page is accessible', async ({ page }) => {
  37 |     await loginAsCustomer(page);
  38 |     await page.goto('/cart');
  39 |     await expect(page.getByText(/cart|empty|item/i).first()).toBeVisible({ timeout: 10000 });
  40 |   });
  41 | 
  42 |   test('my orders page loads', async ({ page }) => {
  43 |     await loginAsCustomer(page);
  44 |     await page.goto('/customer/master-orders');
  45 |     await expect(page.getByText(/order|no order/i).first()).toBeVisible({ timeout: 10000 });
  46 |   });
  47 | 
  48 |   test('notifications page loads', async ({ page }) => {
  49 |     await loginAsCustomer(page);
  50 |     await page.goto('/customer/notifications');
  51 |     await expect(page.getByText(/notification|inbox/i).first()).toBeVisible({ timeout: 10000 });
  52 |   });
  53 | 
  54 |   test('customer profile page loads', async ({ page }) => {
  55 |     await loginAsCustomer(page);
  56 |     await page.goto('/customer/profile');
  57 |     await expect(page.getByText(/profile|account|name|email/i).first()).toBeVisible({ timeout: 10000 });
  58 |   });
  59 | 
  60 | });
  61 | 
```