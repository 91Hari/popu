# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: notifications.spec.ts >> Notification Flow >> caterer notifications page renders
- Location: tests/e2e/notifications.spec.ts:21:7

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected pattern: /\/caterer/
Received string:  "http://localhost:5173/login"
Timeout: 15000ms

Call log:
  - Expect "toHaveURL" with timeout 15000ms
    34 × unexpected value "http://localhost:5173/login"

```

```yaml
- img "PO.PU"
- text: PO.PU pure · fresh · trusted
- heading "Welcome back!" [level=5]
- paragraph: Sign in with your email or mobile number
- alert: "{\"error\":\"email and password are required\"}"
- text: Email or Mobile Number
- textbox "Email or Mobile Number":
  - /placeholder: email@example.com or 10-digit mobile
  - text: caterer@test.com
- text: Password
- textbox "Password": test123
- button
- link "Forgot password?":
  - /url: /forgot-password
- button "Sign In"
- paragraph:
  - text: Don't have an account?
  - link "Register":
    - /url: /register
```

# Test source

```ts
  1  | import { test, expect, Page } from '@playwright/test';
  2  | 
  3  | const CUST_EMAIL    = process.env.PLAYWRIGHT_CUSTOMER_EMAIL    || '';
  4  | const CUST_PASSWORD = process.env.PLAYWRIGHT_CUSTOMER_PASSWORD || '';
  5  | const ADMIN_EMAIL    = process.env.PLAYWRIGHT_ADMIN_EMAIL    || '';
  6  | const ADMIN_PASSWORD = process.env.PLAYWRIGHT_ADMIN_PASSWORD || '';
  7  | 
  8  | test.describe('Notification Flow', () => {
  9  | 
  10 |   test('customer notifications page renders', async ({ page }) => {
  11 |     test.skip(!CUST_EMAIL, 'PLAYWRIGHT_CUSTOMER_EMAIL not set');
  12 |     await page.goto('/login');
  13 |     await page.getByLabel(/email or mobile/i).fill(CUST_EMAIL);
  14 |     await page.getByLabel(/password/i).fill(CUST_PASSWORD);
  15 |     await page.getByRole('button', { name: /sign in|log in/i }).click();
  16 |     await expect(page).toHaveURL(/\/customer/, { timeout: 15000 });
  17 |     await page.goto('/customer/notifications');
  18 |     await expect(page.getByText(/notification|inbox|alert/i).first()).toBeVisible({ timeout: 10000 });
  19 |   });
  20 | 
  21 |   test('caterer notifications page renders', async ({ page }) => {
  22 |     const catEmail = process.env.PLAYWRIGHT_CATERER_EMAIL || '';
  23 |     const catPass  = process.env.PLAYWRIGHT_CATERER_PASSWORD || '';
  24 |     test.skip(!catEmail, 'PLAYWRIGHT_CATERER_EMAIL not set');
  25 |     await page.goto('/login');
  26 |     await page.getByLabel(/email or mobile/i).fill(catEmail);
  27 |     await page.getByLabel(/password/i).fill(catPass);
  28 |     await page.getByRole('button', { name: /sign in|log in/i }).click();
> 29 |     await expect(page).toHaveURL(/\/caterer/, { timeout: 15000 });
     |                        ^ Error: expect(page).toHaveURL(expected) failed
  30 |     await page.goto('/caterer/notifications');
  31 |     await expect(page.getByText(/notification|inbox/i).first()).toBeVisible({ timeout: 10000 });
  32 |   });
  33 | 
  34 |   test('admin broadcast notification page loads', async ({ page }) => {
  35 |     test.skip(!ADMIN_EMAIL, 'PLAYWRIGHT_ADMIN_EMAIL not set');
  36 |     await page.goto('/login');
  37 |     await page.getByLabel(/email or mobile/i).fill(ADMIN_EMAIL);
  38 |     await page.getByLabel(/password/i).fill(ADMIN_PASSWORD);
  39 |     await page.getByRole('button', { name: /sign in|log in/i }).click();
  40 |     await expect(page).toHaveURL(/\/admin/, { timeout: 15000 });
  41 |     await page.goto('/admin/notifications');
  42 |     await expect(page.getByText(/notification|broadcast|message/i).first()).toBeVisible({ timeout: 10000 });
  43 |   });
  44 | 
  45 | });
  46 | 
```