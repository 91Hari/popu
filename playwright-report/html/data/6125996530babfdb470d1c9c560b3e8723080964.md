# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: payment.spec.ts >> Payment Flow >> checkout page renders payment options when cart is populated
- Location: tests/e2e/payment.spec.ts:18:7

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: expect(page).toHaveURL(expected) failed

Expected pattern: /\/customer/
Received string:  "http://localhost:5173/login"
Timeout: 15000ms

Call log:
  - Expect "toHaveURL" with timeout 15000ms
    25 × unexpected value "http://localhost:5173/login"

```

```yaml
- img "PO.PU"
- text: PO.PU pure · fresh · trusted
- heading "Welcome back!" [level=5]
- paragraph: Sign in with your email or mobile number
- text: Email or Mobile Number
- textbox "Email or Mobile Number":
  - /placeholder: email@example.com or 10-digit mobile
- text: Password
- textbox "Password"
- button
- link "Forgot password?":
  - /url: /forgot-password
- button "Sign In"
- paragraph:
  - text: Don't have an account?
  - link "Register":
    - /url: /register
```

# Test source

```ts
  1  | import { test, expect, Page } from '@playwright/test';
  2  | 
  3  | const CUST_EMAIL    = process.env.PLAYWRIGHT_CUSTOMER_EMAIL    || '';
  4  | const CUST_PASSWORD = process.env.PLAYWRIGHT_CUSTOMER_PASSWORD || '';
  5  | 
  6  | async function loginAsCustomer(page: Page) {
  7  |   await page.goto('/login');
  8  |   await page.getByLabel(/email or mobile/i).fill(CUST_EMAIL);
  9  |   await page.getByLabel(/password/i).fill(CUST_PASSWORD);
  10 |   await page.getByRole('button', { name: /sign in|log in/i }).click();
> 11 |   await expect(page).toHaveURL(/\/customer/, { timeout: 15000 });
     |                      ^ Error: expect(page).toHaveURL(expected) failed
  12 | }
  13 | 
  14 | test.describe('Payment Flow', () => {
  15 | 
  16 |   test.skip(!CUST_EMAIL, 'PLAYWRIGHT_CUSTOMER_EMAIL not set — skipping payment tests');
  17 | 
  18 |   test('checkout page renders payment options when cart is populated', async ({ page }) => {
  19 |     await loginAsCustomer(page);
  20 |     await page.goto('/cart');
  21 |     // Check if cart has items — if not, skip the checkout step
  22 |     const emptyIndicator = await page.getByText(/empty|no item/i).count();
  23 |     if (emptyIndicator > 0) {
  24 |       test.skip(true, 'Cart is empty — add items to test checkout payment options');
  25 |     }
  26 |     await page.goto('/checkout/split');
  27 |     await expect(page.getByText(/cod|cash on delivery|qr|payment/i).first()).toBeVisible({ timeout: 10000 });
  28 |   });
  29 | 
  30 |   test('COD payment option is visible on checkout', async ({ page }) => {
  31 |     await loginAsCustomer(page);
  32 |     await page.goto('/checkout/split');
  33 |     const codEl = page.getByText(/cod|cash on delivery/i).first();
  34 |     const vis   = await codEl.isVisible().catch(() => false);
  35 |     if (!vis) {
  36 |       test.skip(true, 'COD option not visible — may require cart items');
  37 |     }
  38 |     await expect(codEl).toBeVisible();
  39 |   });
  40 | 
  41 |   test('payments history page is accessible to customer', async ({ page }) => {
  42 |     await loginAsCustomer(page);
  43 |     await page.goto('/customer/master-orders');
  44 |     await expect(page.getByText(/order|payment|status/i).first()).toBeVisible({ timeout: 10000 });
  45 |   });
  46 | 
  47 | });
  48 | 
```