# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: caterer.spec.ts >> Caterer Flow >> caterer orders page loads
- Location: tests/e2e/caterer.spec.ts:35:7

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected pattern: /\/caterer/
Received string:  "http://localhost:5173/login"
Timeout: 15000ms

Call log:
  - Expect "toHaveURL" with timeout 15000ms
    34 × unexpected value "http://localhost:5173/login"

```

```yaml
- img "PO.PU"
- text: PO.PU pure · fresh · trusted
- heading "Welcome back!" [level=5]
- paragraph: Sign in with your email or mobile number
- alert: "{\"error\":\"email and password are required\"}"
- text: Email or Mobile Number
- textbox "Email or Mobile Number":
  - /placeholder: email@example.com or 10-digit mobile
  - text: caterer@test.com
- text: Password
- textbox "Password": test123
- button
- link "Forgot password?":
  - /url: /forgot-password
- button "Sign In"
- paragraph:
  - text: Don't have an account?
  - link "Register":
    - /url: /register
```

# Test source

```ts
  1  | import { test, expect, Page } from '@playwright/test';
  2  | 
  3  | const EMAIL    = process.env.PLAYWRIGHT_CATERER_EMAIL    || '';
  4  | const PASSWORD = process.env.PLAYWRIGHT_CATERER_PASSWORD || '';
  5  | 
  6  | async function loginAsCaterer(page: Page) {
  7  |   await page.goto('/login');
  8  |   await page.getByLabel(/email or mobile/i).fill(EMAIL);
  9  |   await page.getByLabel(/password/i).fill(PASSWORD);
  10 |   await page.getByRole('button', { name: /sign in|log in/i }).click();
> 11 |   await expect(page).toHaveURL(/\/caterer/, { timeout: 15000 });
     |                      ^ Error: expect(page).toHaveURL(expected) failed
  12 | }
  13 | 
  14 | test.describe('Caterer Flow', () => {
  15 | 
  16 |   test.skip(!EMAIL, 'PLAYWRIGHT_CATERER_EMAIL not set — skipping caterer tests');
  17 | 
  18 |   test('caterer dashboard loads', async ({ page }) => {
  19 |     await loginAsCaterer(page);
  20 |     await expect(page.getByText(/dashboard|caterer|order/i).first()).toBeVisible({ timeout: 10000 });
  21 |   });
  22 | 
  23 |   test('food management page loads', async ({ page }) => {
  24 |     await loginAsCaterer(page);
  25 |     await page.goto('/caterer/foods');
  26 |     await expect(page.getByText(/food|menu|item/i).first()).toBeVisible({ timeout: 10000 });
  27 |   });
  28 | 
  29 |   test('add food page renders form', async ({ page }) => {
  30 |     await loginAsCaterer(page);
  31 |     await page.goto('/caterer/add-food');
  32 |     await expect(page.getByRole('textbox').first()).toBeVisible({ timeout: 10000 });
  33 |   });
  34 | 
  35 |   test('caterer orders page loads', async ({ page }) => {
  36 |     await loginAsCaterer(page);
  37 |     await page.goto('/caterer/sub-orders');
  38 |     await expect(page.getByText(/order|no order/i).first()).toBeVisible({ timeout: 10000 });
  39 |   });
  40 | 
  41 |   test('caterer notifications page loads', async ({ page }) => {
  42 |     await loginAsCaterer(page);
  43 |     await page.goto('/caterer/notifications');
  44 |     await expect(page.getByText(/notification|inbox/i).first()).toBeVisible({ timeout: 10000 });
  45 |   });
  46 | 
  47 |   test('caterer availability page loads', async ({ page }) => {
  48 |     await loginAsCaterer(page);
  49 |     await page.goto('/caterer/availability');
  50 |     await expect(page.getByText(/available|status|open|closed/i).first()).toBeVisible({ timeout: 10000 });
  51 |   });
  52 | 
  53 | });
  54 | 
```