# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth.spec.ts >> Authentication >> admin can logout
- Location: tests/e2e/auth.spec.ts:44:7

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected pattern: /\/admin/
Received string:  "http://localhost:5173/login"
Timeout: 15000ms

Call log:
  - Expect "toHaveURL" with timeout 15000ms
    34 × unexpected value "http://localhost:5173/login"

```

```yaml
- img "PO.PU"
- text: PO.PU pure · fresh · trusted
- heading "Welcome back!" [level=5]
- paragraph: Sign in with your email or mobile number
- alert: "{\"error\":\"email and password are required\"}"
- text: Email or Mobile Number
- textbox "Email or Mobile Number":
  - /placeholder: email@example.com or 10-digit mobile
  - text: admin@popu.com
- text: Password
- textbox "Password": Admin@123
- button
- link "Forgot password?":
  - /url: /forgot-password
- button "Sign In"
- paragraph:
  - text: Don't have an account?
  - link "Register":
    - /url: /register
```

# Test source

```ts
  1  | import { test, expect, Page } from '@playwright/test';
  2  | 
  3  | const ADMIN_EMAIL    = process.env.PLAYWRIGHT_ADMIN_EMAIL    || '';
  4  | const ADMIN_PASSWORD = process.env.PLAYWRIGHT_ADMIN_PASSWORD || '';
  5  | const CUST_EMAIL     = process.env.PLAYWRIGHT_CUSTOMER_EMAIL    || '';
  6  | const CUST_PASSWORD  = process.env.PLAYWRIGHT_CUSTOMER_PASSWORD || '';
  7  | 
  8  | async function fillLogin(page: Page, email: string, password: string) {
  9  |   await page.goto('/login');
  10 |   await page.getByLabel(/email or mobile/i).fill(email);
  11 |   await page.getByLabel(/password/i).fill(password);
  12 |   await page.getByRole('button', { name: /sign in|log in|login/i }).click();
  13 | }
  14 | 
  15 | test.describe('Authentication', () => {
  16 | 
  17 |   test('login page shows email and password fields', async ({ page }) => {
  18 |     await page.goto('/login');
  19 |     await expect(page.getByLabel(/email or mobile/i)).toBeVisible();
  20 |     await expect(page.getByLabel(/password/i)).toBeVisible();
  21 |     await expect(page.getByRole('button', { name: /sign in|log in/i })).toBeVisible();
  22 |   });
  23 | 
  24 |   test('rejects invalid credentials', async ({ page }) => {
  25 |     await fillLogin(page, 'nobody@notreal.invalid', 'wrongpassword123');
  26 |     await expect(page.locator('text=/invalid|incorrect|error|wrong|failed/i').first())
  27 |       .toBeVisible({ timeout: 8000 });
  28 |     await expect(page).toHaveURL(/\/login/);
  29 |   });
  30 | 
  31 |   test('admin login redirects to /admin dashboard', async ({ page }) => {
  32 |     test.skip(!ADMIN_EMAIL, 'PLAYWRIGHT_ADMIN_EMAIL not set');
  33 |     await fillLogin(page, ADMIN_EMAIL, ADMIN_PASSWORD);
  34 |     await expect(page).toHaveURL(/\/admin/, { timeout: 15000 });
  35 |     await expect(page.getByText(/dashboard/i).first()).toBeVisible();
  36 |   });
  37 | 
  38 |   test('customer login redirects to /customer', async ({ page }) => {
  39 |     test.skip(!CUST_EMAIL, 'PLAYWRIGHT_CUSTOMER_EMAIL not set');
  40 |     await fillLogin(page, CUST_EMAIL, CUST_PASSWORD);
  41 |     await expect(page).toHaveURL(/\/customer/, { timeout: 15000 });
  42 |   });
  43 | 
  44 |   test('admin can logout', async ({ page }) => {
  45 |     test.skip(!ADMIN_EMAIL, 'PLAYWRIGHT_ADMIN_EMAIL not set');
  46 |     await fillLogin(page, ADMIN_EMAIL, ADMIN_PASSWORD);
> 47 |     await expect(page).toHaveURL(/\/admin/, { timeout: 15000 });
     |                        ^ Error: expect(page).toHaveURL(expected) failed
  48 |     await page.getByRole('button', { name: /log out|logout|sign out/i }).click();
  49 |     await expect(page).toHaveURL(/\/login/, { timeout: 10000 });
  50 |   });
  51 | 
  52 | });
  53 | 
```