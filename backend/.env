PORT=3000
NODE_ENV=development

DB_HOST=localhost
DB_PORT=5432
DB_NAME=popu
DB_USER=popu
DB_PASSWORD=popu_secret
DB_POOL_MAX=10

JWT_SECRET=popu_jwt_super_secret_change_in_production
JWT_EXPIRES_IN=7d

# LOCAL DEV: allow all origins. PRODUCTION (Render): set to https://popu.co.in
CORS_ORIGIN=*

# LOCAL DEV: localhost. PRODUCTION (Render): set to https://popu.co.in
FRONTEND_URL=http://localhost:5173

# ── Razorpay ─────────────────────────────────────────────────────────────────
RAZORPAY_KEY_ID=rzp_live_TDci2QkzOYdj6y
RAZORPAY_KEY_SECRET=M6xyyoceE1FBPb48ur6YOYAa

# ── SMTP Email Configuration ─────────────────────────────────────────────────
# Set these to enable real email delivery. Without them, reset links are
# logged to the server console (safe for local development).
#
# Gmail example:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tharikrishna1991@gmail.com
SMTP_PASSWORD=zjmltrnjxkchpxav
SMTP_FROM_EMAIL=noreply@popu.co.in
SMTP_FROM_NAME=PO.PU Support
#
# SendGrid example:
#   SMTP_HOST=smtp.sendgrid.net
#   SMTP_PORT=587
#   SMTP_USER=apikey
#   SMTP_PASSWORD=SG.xxxxxxxxxxxxxxxxxxxx
#
# Mailgun example:
#   SMTP_HOST=smtp.mailgun.org
#   SMTP_PORT=587
#   SMTP_USER=postmaster@mg.yourdomain.com
#   SMTP_PASSWORD=your-mailgun-smtp-password


